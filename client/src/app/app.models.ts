export interface Stadium {
  name: string;
  city: string;
  zip_code: string;
  capacity: number;
}
 export interface League {
  name: string;
  country: string;
 }

export interface Team {
  name: string;
  established_year: string;
}
